# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Purpose

This is a **GitHub profile README repository** (`GilRossi/GilRossi`). The sole file is `README.md`, which GitHub renders automatically as Gil Rossi's public profile page at github.com/GilRossi.

There are no build systems, tests, dependencies, or source code — only Markdown with inline HTML for layout and badge images.

## About Gil Rossi

Backend Java Developer, Software Engineer, and Digital Entrepreneur. Main stack: Java 21 + Spring Boot, PostgreSQL, REST APIs, microservices, Docker, AWS, GCP. Also works with Python, JavaScript, React, and Flutter.

Currently studying and actively using:
- **Flutter** — mobile development
- **n8n** — AI workflow automation (self-hostable, 400+ integrations)
- **AI Agents** — Claude Code (Anthropic) and OpenAI Codex CLI for development workflows
- **Google AI Studio** — Gemini API experimentation and prototyping
- **Google Cloud Console** — infrastructure, Vision API, Vertex AI, production deploys

## Editing Guidelines

- The README is written in **Portuguese (pt-BR)**. Keep all additions in the same language.
- Technology icons use `cdn.jsdelivr.net/gh/devicons/devicon` URLs — follow the same pattern for languages/frameworks that have devicons.
- For tools without devicons (n8n, Claude Code, OpenAI, Google AI Studio), use `shields.io` badges with `style=for-the-badge`.
- The projects table links to repositories under `github.com/GilRossi` — verify repos exist before adding new entries.
- Inline HTML (`<div>`, `<img>`) is intentional — GitHub profile renders it correctly for layout that Markdown alone cannot achieve.
